{
  "name": "UserCoins",
  "type": "object",
  "properties": {
    "coins": {
      "type": "integer",
      "default": 0
    }
  },
  "required": [
    "coins"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}